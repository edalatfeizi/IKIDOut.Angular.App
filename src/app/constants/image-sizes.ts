import { BaseUrl } from "./ApiConstants"

export const imagesBaseUrl = BaseUrl
export const IMAGES_SIZES = {
  small: `${imagesBaseUrl}/w185`,
  medium: `${imagesBaseUrl}/w342`,
  large: `${imagesBaseUrl}/original`,
}
